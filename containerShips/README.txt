This is for spotting whether or not an image is a Container Ship. 

Deploy Script (deploy.py) for deploying the CNN (LeNet).

Train (train.py) for training the CNN (LeNet).

Model (model.model) a pre-trained model that can be used and compared.
